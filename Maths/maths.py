#so there is a maths module containing many mahts functions that we can utilize
import math


x = set((1,2,3,4,5))

y = min(x) # the min funtion finds the smallest number in a collection
print(y)

y = max(1,2,3,4,5)# the max one finds the biggeset number in the collection
print(y)

y = abs(-29288) #abs returns the absolutre positive number 
print(y)

y = pow(3,5) #pow stands for power of word 3 times 5 ( 3x3x3x3x3)
print(y)

"""Now lets use some futnions from math library"""
x = math.sqrt(64)
print(x)

y = math.ceil(1.5)# it returns the closest integer to the number that comes after that num
z = math.floor(1.5)# it returns the closest integer to the number that comes before that num

x= math.pi
print(x)